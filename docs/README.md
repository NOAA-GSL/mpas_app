To install into an activated conda environment the dependencies needed to build docs:

```
. install-deps
```

To build docs:

```
make docs
```

To view docs in browser, visit URL

```
file://<path-to-this-directory>/build/html/index.html
```
